# Root of the dataset view (the folder that directly holds DPDD/, RealDOF/, ...).
# Relative paths resolve against the repo root; absolute paths are used as-is.
# Default (unset) is `data`. Point this elsewhere to keep datasets outside the repo.
DATA_ROOT=./data
